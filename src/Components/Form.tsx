import {ReactElement} from "react";

const Form = (): ReactElement => {
    return (<form>
        <div>
            <label>Enter name</label>
            <input/>
        </div>
        <div>
            <label>Enter age</label>
            <input/>
        </div>
    </form>)
}

export default Form;
